{
  "name": "ts-web",
  "private": true,
  "version": "1.0.0",
  "description": "nodejs backend with typescript",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "prestart": "npm run build",
    "start": "nodemon dist localhost 8080",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {},
  "devDependencies": {
    "nodemon": "^3.1.9",
    "typescript": "^5.7.2",
    "@types/node": "^22.10.5"
  },
  "keywords": []
}
